// connectors/clickhouse.js
// TODO: Phase 2
// npm install @clickhouse/client
// Will use AWS named profiles — no terminal config changes needed
// process.env.AWS_PROFILE = config.aws[region].profile  ← set per task

// export async function runQuery(_queryDef, _region = 'us') {
//     throw new Error('[clickhouse] Connector not yet implemented. Coming in Phase 2.');
//   }
  